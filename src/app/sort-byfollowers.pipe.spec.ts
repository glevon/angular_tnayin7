import { SortByfollowersPipe } from './sort-byfollowers.pipe';

describe('SortByfollowersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByfollowersPipe();
    expect(pipe).toBeTruthy();
  });
});
